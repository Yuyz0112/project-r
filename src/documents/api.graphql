query getApps {
  apps {
    id
    name
    sessions {
      id,
      lastEventTime,
      createdAt
    }
    createdAt
  }
}

query getEvents($sessionId: String!) {
  events(sessionId: $sessionId) {
    timestamp
    type
    data
  }
}
